# export-service

Exporta dados decodificados em multiplos formatos. Roda jobs assincronos.

## Formatos suportados (planejados)

GeoJSON, GeoParquet (para Spark/DuckDB), NetCDF (padrao meteorologia), Shapefile, CSV.

Meta: campo cheio do Eta-3km exportado em <= 30s, com browser responsivo.
